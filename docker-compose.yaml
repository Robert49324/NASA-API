version: '3.9'

services:
  postgres:
      image: postgres:latest
      environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: 0825
        POSTGRES_DB: test_db
      ports:
        - 5432:5432
      restart: always

